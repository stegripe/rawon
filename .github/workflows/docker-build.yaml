name: Docker Build

on:
  release:
    types:
      - created
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  docker:
    uses: stegripe/workflows/.github/workflows/docker-build-and-push.yaml@70f2b51880ed314ac9a98c2cd2de6582668c4a9a # main
