name: Docker Build

on:
  release:
    types:
      - created
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  docker:
    uses: stegripe/workflows/.github/workflows/docker-build-and-push.yaml@main # main
