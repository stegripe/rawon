name: Docker Build

on:
  release:
    types:
      - created
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  docker:
    uses: stegripe/workflows/.github/workflows/docker-build-and-push.yaml@fcfd6bae08c7afa0a213fae4584a8667f0214688 # main
