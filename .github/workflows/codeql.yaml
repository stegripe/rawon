name: CodeQL

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  schedule:
    - cron: "0 3 * * 0"
  workflow_dispatch:

jobs:
  codeql:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: stegripe/workflows/.github/workflows/codeql-javascript-typescript.yaml@70f2b51880ed314ac9a98c2cd2de6582668c4a9a # main
    with:
      runner: self-hosted
      node-version-file: .node-version
      build-mode: none