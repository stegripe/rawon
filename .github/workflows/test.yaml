name: Lint Code and Compile

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  test:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-test.yaml@a779ff9f1fd861c2f4426d090ef3c98ec3bf3426 # main
    secrets: inherit
