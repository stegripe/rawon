name: Build

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  build:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-compile.yaml@fcfd6bae08c7afa0a213fae4584a8667f0214688 # main
