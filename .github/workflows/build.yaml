name: Build

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  build:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-compile.yaml@70f2b51880ed314ac9a98c2cd2de6582668c4a9a # main
