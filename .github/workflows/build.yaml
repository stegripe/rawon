name: Build

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  build:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-compile.yaml@356dbc21510fc935c351c19efa7a8545fe276c41 # main
    secrets: inherit
