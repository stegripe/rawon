name: Build

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  build:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-compile.yaml@main # main
